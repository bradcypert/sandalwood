---
title: "{{ replace .Name "-" " " | title }}"
date: {{ .Date }}
draft: true
# Optional. Shown as a collapsed "How AI was used" note on the post.
# String, markdown, a list of strings, or `true` for a generic line.
# ai_assisted: "I used Claude when researching X and generating the chart."
---

